import { Container as MuiContainer, styled } from "@mui/material";

export const PartnersContainer = styled(MuiContainer)(() => ({
  display: "flex",
  justifyContent: "space-between",
  marginBottom: "50px",
}));
